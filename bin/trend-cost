#!/bin/zsh

tee >(stdbuf -o0 grep -i cost |
          stdbuf -o0 cut -f 2 |
          trend -t "${TREND_TITLE:-Cost}" -c 1a -geometry ${TREND_GEOMETRY:-320x64-0-24} - ${TREND_SIZE:-$((60*4*5))} ${TREND_SCALE})

[[ -z `jobs -p` ]] || kill $(jobs -p)
